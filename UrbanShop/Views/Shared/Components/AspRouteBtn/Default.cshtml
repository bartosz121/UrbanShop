@model UrbanShop.ViewComponents.AspRouteBtn.AspRouteBtnContext

<div class="@Model.wrapperClass border border-gray-300 hover:border-gray-500 transition ease-in-out">
    <a asp-controller="@Model.controller" asp-action="@Model.action" class="inline-block py-2 px-4 uppercase no-underline hover:text-black hover:underline hover:bg-gray-400 hover:bg-opacity-5">@Model.btnText</a>
</div>